This is just a small implementation of the square-1 and its features in python, with the hopes of
doing a full state-to-state solver. The code is self-explanatory for the most part.

Right now it's doing recursively which takes a while and it's not optimal.
